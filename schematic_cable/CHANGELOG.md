# Changelog

## 0.1.0

- Initial extraction from tower_configurator_flutter. Consolidates the previously
  duplicated `WireSpec`/`CableSpec` (from the app's `cable_painter.dart` and
  `cable_breakout.dart`) into a single model.
- Provides `CablePainter` (cross-section), `CableBreakout` widget +
  `CableBreakoutPainter`, `PowerScheme`, and public color palettes.
